package org.openstack.atlas.restclients.auth.config;

import org.openstack.atlas.cfg.ConfigurationKey;

public enum AuthenticationCredentialConfigurationKeys implements ConfigurationKey {
    identity_auth_url,
    identity_user,
    identity_pass
}
