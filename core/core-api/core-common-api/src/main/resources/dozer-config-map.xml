<?xml version="1.0" encoding="UTF-8"?>
<mappings xmlns="http://dozer.sourceforge.net"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://dozer.sourceforge.net
          http://dozer.sourceforge.net/schema/beanmapping.xsd">

    <configuration>
        <stop-on-errors>true</stop-on-errors>
        <!-- default date-format will apply to all class maps unless the class mapping explicitly overrides it -->
        <date-format>MM-dd-yyyy HH:mm:ss</date-format>
        <!-- default wildcard policy that will apply to all class maps unless the class mapping explicitly overrides it -->
        <wildcard>true</wildcard>
        <custom-converters> <!-- these are always bi-directional -->
            <converter type="org.openstack.atlas.api.mapper.dozer.converter.PersistenceConverter">
                <class-a>org.openstack.atlas.core.api.v1.SessionPersistence</class-a>
                <class-b>org.openstack.atlas.service.domain.entity.SessionPersistence</class-b>
            </converter>
        </custom-converters>
    </configuration>

</mappings>